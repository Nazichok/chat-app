// environment.prod is here because render.com lets use env files only on top level
export const environment = {
  production: true,
  serverUrl: 'http://localhost:8080',
  localCryptoKey: 'somelocalkey',
  googleClientId: 'somegoogleclientid.apps.googleusercontent.com',
  vapidPublicKey: 'somekey'
};
